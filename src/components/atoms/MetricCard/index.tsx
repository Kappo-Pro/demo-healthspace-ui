/**
 * MetricCard Atom Component Export
 */

// eslint-disable-next-line react-refresh/only-export-components
export { MetricCard, default } from './MetricCard';
export type { MetricCardProps } from './types';
