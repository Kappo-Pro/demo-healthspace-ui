/**
 * Icon Module Exports
 *
 * Barrel export file for the Icon component system.
 */

// Main component
export { UntitledIcon } from './UntitledIcon';

// Types
export type { UntitledIconProps, IconName, IconSize } from './types';

// Utilities (if needed externally)
export { ICON_SIZES, getIconSize } from './utils';

// Registry (if needed for direct access)
export { IconRegistry } from './iconRegistry';
